cmake_minimum_required(VERSION 3.16)

# This component was renamed to `mqtt_stub` to avoid collision with the
# upstream IDF `mqtt` component. The original sources were copied to
# `components/mqtt_stub/`. Leaving this file empty prevents duplicate
# component registration during the build.

